export class FirstName {
    scale: number;
    firstName: string;
    sex: string;

    constructor(scale: number, firstName: string, sex: string) {
        this.scale = scale;
        this.firstName = firstName;
        this.sex = sex;
    }
}

export class LastName {
    scale: number;
    lastName: string;
    sex: string;

    constructor(scale: number, lastName: string, sex: string) {
        this.scale = scale;
        this.lastName = lastName;
        this.sex = sex;
    }
}

